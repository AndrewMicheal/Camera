import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.code.camera',
  appName: 'camera',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
